/** Parametreli mesajlar (örn. "${n} bölüm yüklendi"). */
export type LangFn = (...args: Array<string | number>) => string;

/** Bir dilin tam anahtar seti — eksik anahtar derleme hatasıdır. */
export interface Lang {
  search_placeholder: string;
  btn_search: string;
  btn_back: string;
  now_playing: string;
  ep_fallback: LangFn;
  pick_episode: string;
  btn_prev: string;
  btn_next: string;
  btn_sort: string;
  filter_placeholder: string;
  settings_title: string;
  settings_heading: string;
  close: string;
  sort_asc_label: string;
  sort_desc_label: string;
  ep_saved_badge: string;
  status_loading: string;
  status_ok: LangFn;
  status_err: string;
  searching: string;
  no_results: string;
  ep_count_unit: string;
  ep_not_found: string;
  loading_eps: string;
  retry: string;
  btn_retry: string;
  btn_queue: string;
  sec_podcasts: string;
  queued: string;
  confirm_clear_progress: string;
  confirm_clear_all: string;
  dl_not_found: string;
  dl_no_space: string;
  s_lang_title: string;
  s_lang_label: string;
  s_playback: string;
  s_default_speed: string;
  s_speed_sub: string;
  s_skip_back: string;
  s_skip_fwd: string;
  s_auto_next: string;
  s_resume: string;
  s_appearance: string;
  s_accent: string;
  s_font_size: string;
  s_row_height: string;
  s_theme_label: string;
  s_list: string;
  s_default_sort: string;
  s_show_dl: string;
  s_data: string;
  btn_clear_progress: string;
  btn_clear_all: string;
  theme_auto: string;
  theme_dark: string;
  theme_light: string;
  theme_oled: string;
  size_small: string;
  size_normal: string;
  size_large: string;
  size_xlarge: string;
  row_compact: string;
  row_spacious: string;
  sort_asc: string;
  sort_desc: string;
  dir: 'ltr' | 'rtl';
  audio_err: string;
  private_feed_err: string;
  proxies_disabled_err: string;
  s_privacy: string;
  s_allow_proxies: string;
  s_allow_proxies_sub: string;
  s_prefetch: string;
  s_prefetch_sub: string;
  s_prefetch_always: string;
  s_prefetch_wifi: string;
  s_prefetch_never: string;
  dur_h: string;
  dur_m: string;
  storage_full: string;
  favs_title: string;
  fav_btn: string;
  share_btn: string;
  link_copied: string;
  sleep_timer: string;
  play: string;
  pause: string;
  seek_label: string;
  speed_label: string;
  sleep_set: LangFn;
  sleep_done: string;
  sleep_end_of_episode: string;
  sleep_custom: string;
  sleep_custom_label: string;
  sleep_set_episode: string;
  sleep_off: string;
  sleep_extended: string;
  sleep_remaining: string;
  sc_title: string;
  sc_playpause: string;
  sc_back: string;
  sc_forward: string;
  sc_prev: string;
  sc_next: string;
  sc_seek_edges: string;
  sc_close: string;
  sc_help: string;
  s_ambient: string;
  s_ambient_hint: string;
  np_notes: string;
  dl_label: string;
  rss_invalid: string;
  limit_note: string;
  limit_of_total: LangFn;
  s_storage: string;
  storage_usage: LangFn;
  btn_opml_export: string;
  btn_opml_import: string;
  btn_json_export: string;
  btn_json_import: string;
  json_imported: string;
  json_invalid: string;
  btn_clear_downloads: string;
  opml_imported: LangFn;
  opml_invalid: string;
  dl_saved: string;
  dl_removed: string;
  dl_opened_tab: string;
  confirm_title: string;
  confirm_ok: string;
  confirm_cancel: string;
  confirm_clear_downloads: string;
  confirm_unsubscribe: string;
  queue_title: string;
  queue_empty: string;
  queue_clear: string;
  queue_remove: string;
  queue_move_up: string;
  queue_move_down: string;
  offline_banner: string;
  toast_opml_exported: string;
  toast_json_exported: string;
  lang_select_label: string;
  /* ── 4.0 "Sinyal" IA ── */
  nav_home: string;
  nav_search: string;
  nav_library: string;
  nav_settings: string;
  nav_collapse: string;
  nav_expand: string;
  volume_label: string;
  mute: string;
  unmute: string;
  sc_sidebar: string;
  home_tagline: string;
  home_continue: string;
  home_empty: string;
  home_empty_hint: string;
  home_see_all: string;
  lib_tab_subs: string;
  lib_tab_downloads: string;
  lib_empty_subs: string;
  lib_empty_subs_hint: string;
  lib_empty_downloads: string;
  lib_delete_download: string;
  lib_dl_total: LangFn;
  np_open: string;
  np_close: string;

  /* cross-device sync */
  s_sync: string;
  s_sync_state: string;
  s_sync_code: string;
  s_sync_code_sub: string;
  s_sync_code_ph: string;
  btn_sync_start: string;
  btn_sync_link: string;
  btn_sync_link_go: string;
  btn_sync_now: string;
  btn_sync_copy: string;
  btn_sync_unlink: string;
  btn_sync_forget: string;
  sync_not_linked: string;
  sync_idle: string;
  sync_syncing: string;
  sync_ok: string;
  sync_error: string;
  sync_unavailable: string;
  sync_unreadable: string;
  toast_sync_linked: string;
  toast_sync_bad_code: string;
  toast_sync_copied: string;
  toast_sync_copy_failed: string;
  toast_sync_forgotten: string;
  confirm_sync_forget: string;
  /* crash recovery (ui/fatal.ts) */
  fatal_title: string;
  fatal_body: string;
  fatal_reload: string;
  fatal_wipe: string;
  fatal_wipe_confirm: string;
  fatal_details: string;
  /* episode state filters + played marks */
  filter_all: string;
  filter_unplayed: string;
  filter_inprogress: string;
  filter_downloaded: string;
  filter_hidden: LangFn;
  filter_no_match: string;
  mark_played: string;
  mark_unplayed: string;
  toast_marked_played: string;
  toast_marked_unplayed: string;
  /* new episodes (feeds/inbox.ts) */
  home_new: string;
  home_new_clear: string;
  home_new_dismiss: string;
  home_checking: string;
  home_check_now: string;
  toast_new_found: LangFn;
  toast_no_new: string;
  /* episode sharing (A4) */
  share_episode: string;
  share_episode_at: LangFn;
  link_copied_at: LangFn;
  /* per-show speed (A5) + download progress (A8) */
  s_feed_speeds: string;
  s_feed_speeds_sub: LangFn;
  btn_reset_feed_speeds: string;
  toast_feed_speeds_reset: string;
  dl_cancel: string;
  dl_cancelled: string;
  dl_progress: LangFn;
  dl_working: string;
  /* chapters and transcripts (A6) */
  np_chapters: LangFn;
  np_transcript: string;
  np_transcript_loading: string;
  np_transcript_failed: string;
  chapter_jump: LangFn;
  chapter_untitled: string;
  /* incremental episode rendering (B2) */
  ep_show_more: LangFn;
  /* episode row label (C4) */
  ep_open: LangFn;
  /* list keyboard model (C4) */
  sc_list: string;
  /* diagnostics (C5) */
  btn_diagnostics: string;
  toast_diagnostics: string;
  storage_feeds: LangFn;
  /* auto-download (A7) */
  s_auto_download: string;
  s_auto_download_sub: string;
  s_delete_played: string;
  s_delete_played_sub: string;
  /* discovery by topic (A9) */
  /* discovery by topic (A9) — each value is BOTH the chip label and the search term */
  sec_browse: string;
  topic_news: string;
  topic_history: string;
  topic_science: string;
  topic_tech: string;
  topic_culture: string;
  topic_comedy: string;
  topic_sport: string;
  topic_health: string;
}

export type LangKey = keyof Lang;
export type LangCode = 'tr' | 'en' | 'de' | 'fr' | 'es' | 'ar' | 'ja' | 'ru';
