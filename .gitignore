# Dependencies / build output (none are committed — this app has no build step)
node_modules/
dist/

# Logs
*.log
npm-debug.log*

# Environment (local worker URL is not a secret — keep it versioned)
.env
.env.*
!.env.development

# Wrangler local state
.wrangler/

# Editor / OS
.vscode/
.idea/
.DS_Store
thumbs.db

# Tooling
.claude/
